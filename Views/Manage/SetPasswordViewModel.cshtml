@model SetPasswordViewModel
@{
    ViewData["Title"] = "Set Password";
    Layout = "_ManageLayout";
}

<div class="manage-card">
    <div class="manage-card-header">
        <h1 class="h3 mb-0"><i class="bi bi-key me-2"></i>Set Password</h1>
    </div>

    <form asp-action="SetPassword" method="post" class="needs-validation" novalidate>
        <div class="mb-3">
            <label asp-for="NewPassword" class="form-label">New Password</label>
            <div class="input-group has-validation">
                <span class="input-group-text"><i class="bi bi-key"></i></span>
                <input asp-for="NewPassword"
                       class="form-control"
                       type="password"
                       required
                       minlength="8"
                       data-password-strength>
                <div class="invalid-feedback">
                    Minimum 8 characters required
                </div>
            </div>
            <div class="password-strength-meter mt-2">
                <div class="strength-bar"></div>
            </div>
        </div>

        <div class="mb-4">
            <label asp-for="ConfirmPassword" class="form-label">Confirm Password</label>
            <div class="input-group has-validation">
                <span class="input-group-text"><i class="bi bi-key-fill"></i></span>
                <input asp-for="ConfirmPassword"
                       class="form-control"
                       type="password"
                       required
                       minlength="8">
                <div class="invalid-feedback">
                    Passwords must match
                </div>
            </div>
        </div>

        <button type="submit" class="btn btn-primary w-100">
            <i class="bi bi-check-circle me-2"></i>Set Password
        </button>
    </form>
</div>

@section Scripts {
    <script nonce="EDNnf03nceIOfn39fn3e9h3sdfa">
        // Reuse password strength indicator from ChangePassword
    </script>
}
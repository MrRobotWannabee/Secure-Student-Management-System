@model ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<div class="container py-5">
    <div class="text-center">
        <h1 class="display-3 text-danger"><i class="fas fa-exclamation-triangle"></i> Oops!</h1>
        <h2 class="text-danger">An error occurred while processing your request.</h2>

        @if (Model.ShowRequestId)
        {
            <p class="mt-4">
                <strong>Request ID:</strong> <code>@Model.RequestId</code>
            </p>
        }

        <hr class="my-4">

        <h3 class="text-warning">What happened?</h3>
        <p>
            Something went wrong while processing your request. This might be a temporary issue or a more complex one on our end.
        </p>

        <h3 class="text-info">How to proceed?</h3>
        <p>
            If the problem persists, you can try the following:
        </p>
        <ul class="text-left">
            <li>Refresh the page to see if the error is resolved.</li>
            <li>Clear your browser cache and cookies.</li>
            <li>Return to the <a href="/">homepage</a> and try again.</li>
        </ul>

        <h3 class="text-info">Development Mode</h3>
        <p class="text-muted">
            If you are a developer or working in a development environment, enabling <strong>Development Mode</strong> will show more detailed error information.
        </p>
        <p class="text-muted">
            <strong>Important:</strong> The <strong>Development environment</strong> should not be enabled in production as it can expose sensitive details about the error to users.
        </p>
        <p>
            For local debugging, enable the <strong>Development</strong> environment by setting the <strong>ASPNETCORE_ENVIRONMENT</strong> environment variable to <strong>Development</strong> and restarting the app.
        </p>

        <div class="mt-5">
            <a href="/" class="btn btn-primary">Go to Homepage</a>
            <a href="mailto:support@yourdomain.com" class="btn btn-secondary">Contact Support</a>
        </div>
    </div>
</div>
